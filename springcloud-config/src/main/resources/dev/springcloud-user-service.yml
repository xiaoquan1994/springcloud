server:
  #服务启动的端口号
  port: 8500
eureka:
  client:
    service-url:
      #eureka的服务注册地址
      defaultZone: http://127.0.0.1:8000/eureka
management:
  endpoints:
    jmx:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
spring:
  zipkin:
    base-url: http://zipkin:9411/
    sender:
      type: web
    sleuth:
      sampler:
        probability: 1
  boot:
    admin:
      client:
        url: http://127.0.0.1:8300